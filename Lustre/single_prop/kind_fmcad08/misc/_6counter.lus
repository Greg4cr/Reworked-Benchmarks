
node top(x: bool)
   returns (OK: bool);

var
   a: bool; 
   b: bool; 
   c: bool; 
   rlt__PRE: bool;


let 
   a = (false -> (not (pre a)));

   b = (false -> ((((not (pre c)) and 
      (not (pre b))) and 
      (pre a)) or 
      ((pre b) and 
      (not (pre a)))));

   c = (false -> (((pre c) and 
      (not (pre a))) or 
      ((pre b) and 
      (pre a))));

   OK = (true -> ((pre (true -> rlt__PRE)) = (not b)));

   rlt__PRE = (pre b);

tel;


